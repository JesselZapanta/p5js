function setup() {
 // put setup code here
  createCanvas(1000, 200);
  
}

function draw() {
  background(200);
  // Draw the red circle.
  // push();
  // translate(100, 100);
  // fill('red');
  // circle(0, 0, 40);
  // pop();

  // push();
  // translate(50, 50);
  // fill('blue');
  // circle(0, 0, 40);
  // pop();
            // 60 * .2
  let speed1 = frameCount * 0.2;
  let speed2 = frameCount * 0.3;

  push();
  translate(speed1, 50);
  circle(0, 0, 40);
  pop();

  push();
  translate(speed2, 50);
  circle(40, 0, 40);
  pop();
}
